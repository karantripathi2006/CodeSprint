# ── Core API ──────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart==0.0.6

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.36

# ── Config & Utilities ────────────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv==1.0.0
aiofiles==23.2.1
slowapi==0.1.9

# ── Auth (were missing — required by security_auth.py) ───────────────────────
bcrypt==4.1.2
PyJWT==2.8.0

# ── Resume Parsing ────────────────────────────────────────────────────────────
PyMuPDF>=1.24.0       # 1.24+ has pre-built wheels; 1.23.x requires building from source
pdfplumber==0.10.3
python-docx>=1.1.0

# ── ML / Embeddings ───────────────────────────────────────────────────────────
# Install CPU-only torch first to avoid 2GB CUDA download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch==2.1.2
sentence-transformers>=2.7.0      # 2.7+ has Python 3.12/3.13 wheels

# ── LangChain + Vector Store ──────────────────────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
# chromadb and langchain-chroma require pyarrow which has no Python 3.14 wheel yet.
# Use Python 3.12 to enable these. The app falls back to hash-based in-memory search.
# langchain-chroma>=0.1.4
# chromadb>=0.5.0

# ── Document Parsing (Docling — richer PDF/DOCX extraction) ──────────────────
# docling>=2.0.0                # IBM's document AI parser; requires libxml2-dev libxslt1-dev — app falls back to PyMuPDF/pdfplumber without it

# ── Async Tasks (optional — app runs in sync mode without these) ───────────────
redis==5.0.1
celery==5.3.6

# ── PostgreSQL (optional — SQLite is the default; only add if using Postgres) ──
psycopg2-binary>=2.9.9          # Required: docker-compose sets DATABASE_URL to PostgreSQL
